# ─────────────────────────────────────────────────────────────
# requirements.txt — SIGERLM: Multilingual LLM with SSM
# Python >= 3.10
# ─────────────────────────────────────────────────────────────

# ── Core Deep Learning ────────────────────────────────────────
torch>=2.2.0                   # CPU build: pip install torch --index-url https://download.pytorch.org/whl/cpu
einops>=0.7.0                  # tensor ops untuk SSM

# ── Tokenizer ────────────────────────────────────────────────
tiktoken>=0.6.0                # OpenAI BPE tokenizer

# ── Dataset & HuggingFace ────────────────────────────────────
datasets>=2.18.0,<3.0.0        # keep 2.x because SEACrowd/NusaCrowd still use dataset scripts
huggingface-hub>=0.21.0,<1.0.0 # download model/dataset
pyarrow>=15.0.0                # parquet backend for HF datasets
fsspec>=2024.2.0,<=2024.6.1    # datasets 2.x compatible filesystem abstraction
dill==0.3.8                    # datasets 2.21 compatible serializer
multiprocess==0.70.16          # datasets 2.21 compatible multiprocessing helper
seacrowd>=0.2.2                # SEACrowd dataset loaders, e.g. SEACrowd/indoqa
nusacrowd>=0.1.4               # NusaCrowd dataset loaders, e.g. SEACrowd/tydiqa_id
kaggle>=1.6.17                 # Kaggle CLI/API for IndoNewsQA download
kagglehub>=0.3.0               # alternate Kaggle dataset downloader

# ── Quantization ─────────────────────────────────────────────
bitsandbytes>=0.43.0           # INT4/INT8 quantization (opsional, GPU only untuk INT4)

# ── ONNX Export & Runtime ────────────────────────────────────
onnx>=1.16.0                   # ONNX model format
onnxruntime>=1.17.0            # CPU inference runtime (2-3x faster dari PyTorch CPU)
onnxruntime-extensions>=0.9.0  # additional ops

# ── API Server ────────────────────────────────────────────────
fastapi>=0.110.0               # REST API framework
uvicorn>=0.29.0                # ASGI server
pydantic>=2.6.0                # request/response validation

# ── Evaluation ───────────────────────────────────────────────
nltk>=3.8.1                    # tokenization untuk BLEU
rouge-score>=0.1.2             # ROUGE metric

# ── Monitoring & Logging ─────────────────────────────────────
psutil>=5.9.0                  # CPU/RAM monitoring
tqdm>=4.66.0                   # progress bar

# ── Utilities ────────────────────────────────────────────────
numpy>=1.26.0
pathlib2>=2.3.7
dataclasses-json>=0.6.4        # config serialization

# ── Optional: Experiment Tracking ────────────────────────────
# wandb>=0.16.0                # uncomment jika mau tracking di WandB
# tensorboard>=2.16.0          # uncomment jika mau TensorBoard

# ── Optional: Faster Inference ───────────────────────────────
# triton>=2.2.0                # custom CUDA kernels (GPU only)
# flash-attn>=2.5.0            # flash attention (GPU only)

# ── Dev Tools ────────────────────────────────────────────────
pytest>=8.0.0
black~=22.0                  # seacrowd 0.2.2 requires black~=22.0
isort>=5.13.0

# ── PDF & Dataset Extraction ─────────────────────────────────
pymupdf>=1.24.0                # extract text dari PDF kamus/paper
pandas>=2.2.0                  # dataset inspection & CSV processing
beautifulsoup4>=4.12.0         # HTML scraping
requests>=2.32.0               # HTTP requests untuk scraping/download
lxml>=5.2.0                    # faster HTML/XML parser for BeautifulSoup
html5lib>=1.1                  # tolerant HTML parser fallback
markdownify>=0.12.1            # optional HTML-to-Markdown conversion for docs mining

# ── Translation & NLP Utilities ──────────────────────────────
sentencepiece>=0.2.0           # optional tokenizer experiments
tokenizers>=0.20.0             # custom GPT-style ByteLevel BPE tokenizer
transformers>=4.45.0,<5.0.0    # PreTrainedTokenizerFast / HF tokenizer tooling
sacremoses>=0.1.1              # SMT preprocessing/tokenization
langdetect>=1.0.9              # language detection

# ── JSONL & Data Processing ──────────────────────────────────
orjson>=3.10.0                 # faster JSON serialization
jsonlines>=4.0.0               # JSONL reader/writer

onnxscript>=0.1.0

